# GitHub Pages

The reference manual deploys to `https://itzfaisal.github.io/dictionary-of-ai-coding/` through `.github/workflows/pages.yml`.

In the repository's **Settings > Pages**, set **Build and deployment > Source** to **GitHub Actions**. This project Pages site does not use a custom domain; leave the custom-domain field empty.
