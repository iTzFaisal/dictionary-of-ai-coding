{
  "name": "ai-coding-dictionary",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "tsx internal/generate-readme.ts",
    "prepare": "husky",
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "typecheck": "astro check",
    "test": "node --import tsx --test tests/**/*.test.ts && npm run build",
    "search": "pagefind --site dist"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^22.10.0",
    "astro": "^7.0.7",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "markdown-it": "^14.3.0",
    "pagefind": "^1.5.2",
    "prettier": "^3.8.3",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
